# Пример конфигурации для асинхронного парсинга
# Скопируйте эти параметры в ваш .env файл

# Async Processing Configuration
# WORKER_COUNT: количество параллельных worker'ов (рекомендуется: 5-20)
# BATCH_SIZE: размер батча ключевых слов (рекомендуется: 25-100)

# Для небольших проектов (до 500 ключевых слов):
# WORKER_COUNT=5
# BATCH_SIZE=25

# Для средних проектов (500-2000 ключевых слов):
# WORKER_COUNT=10
# BATCH_SIZE=50

# Для крупных проектов (2000+ ключевых слов):
# WORKER_COUNT=20
# BATCH_SIZE=100

# Текущие значения (средний проект):
WORKER_COUNT=10
BATCH_SIZE=50
